{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "API: FastAPI (local uvicorn)",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": ["app.main:app", "--reload", "--host", "0.0.0.0", "--port", "8000"],
      "cwd": "${workspaceFolder}/services/api",
      "envFile": "${workspaceFolder}/infra/compose/.env",
      "console": "integratedTerminal"
    }
  ]
}
